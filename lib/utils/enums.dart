enum ItemType { product, service }

enum ItemChangeType { add, edit }

enum ItemOperation { addProduct, editProduct, addService, editService }

enum ClientOperation { addClient, editClient }

enum ClientAction { deleteClient, addCredit, editClient }

enum InvoiceAction { deleteInvoice, createPDF, editInvoice, sendPDF }

enum DiscountType { flat, percentage }

enum ChargeType { additionalCharge, tax }
